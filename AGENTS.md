# AGENTS.md

## Agent skills

### Issue tracker

问题与规格记录在 GitHub Issues（`jiahao07A/glosstake`）。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准分诊标签沿用默认字符串。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
