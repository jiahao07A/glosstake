# Gloss

Gloss 是 B 站视频的字幕列表浏览器扩展：在视频旁展示完整字幕列表，并在此基础上提供逐条翻译与 AI 总结。本文件定义讨论这套系统时使用的领域词汇，避免同一概念出现多种叫法。

## Language

### 字幕

**字幕轨（Info）**：
视频可选的一份字幕，按语言区分；来源可以是 B 站 AI 字幕、UP 主上传或用户本地导入。
_Avoid_: 字幕源、字幕选项、字幕文件

**字幕项（TranscriptItem）**：
字幕的最小单位：一条带起止时间与文本的台词。
_Avoid_: 字幕行、句子、条目

**字幕数据（Transcript）**：
一个字幕轨的完整字幕项序列。
_Avoid_: 字幕文本、字幕内容

**字幕列表**：
按时间顺序排列字幕项的视图，是本产品的核心界面。
_Avoid_: 字幕面板、字幕视图、字幕窗口

**章节（Chapter）**：
视频自带的段落标记，带起止时间与标题。
_Avoid_: 小节、段落、分P

**分段（Segment）**：
一段连续的字幕项，是翻译、总结与提问的处理与展示单位。
_Avoid_: 分块、区块、分组、chunk

**本地字幕**：
用户从本地 `.srt` / `.vtt` 文件导入的字幕轨，与视频自带字幕并列可选。
_Avoid_: 上传文件、自定义字幕

**字幕搜索**：
在字幕列表内按文本检索字幕项，可选用中文拼音匹配。
_Avoid_: 全文搜索、筛选、过滤

### 视频

**视频信息（Video info）**：
当前视频的标题、作者、发布时间与稿件标识。
_Avoid_: 元数据、视频数据

**分P（Pages）**：
B 站同一稿件下可独立播放的多个视频部分，各自有标题与字幕。
_Avoid_: 集、分集、分段

### AI 能力

**翻译（Translation）**：
字幕项文本到用户设定的目标语言的转换，结果按项回填到字幕列表。
_Avoid_: 转译、本地化、字幕转换

**自动翻译（autoTranslate）**：
一种翻译模式：译文随播放进度持续产出，而非按需触发。
_Avoid_: 实时翻译、同步翻译、自动字幕

**译文显示方式（transDisplay）**：
字幕项中原文与译文的排布：仅译文、原文主译文辅、译文主原文辅。
_Avoid_: 显示模式、双语模式

**总结（Summary）**：
AI 针对某个分段生成的产物，带类型与状态。
_Avoid_: 摘要、分析、摘要结果

**概览（Overview）**：
一种总结，内容为带时间戳与 emoji 的要点条目。
_Avoid_: 大纲、提纲

**要点（Keypoint）**：
一种总结，内容为分段要点的完整罗列。
_Avoid_: 关键点、重点、关键信息

**一句话总结（Brief）**：
一种总结，内容为对分段的一句话概括。
_Avoid_: 总结、简短总结、摘要

**问题（Question）**：
一种总结，内容为从分段抽取的常见问答对。
_Avoid_: FAQ、问答、常见问题列表

**辩论（Debate）**：
一种总结，内容为正反双方围绕作者观点的多轮对话。
_Avoid_: 讨论、对话、正反方分析

**提问（Ask）**：
用户针对某个分段自由提出的问题及其 AI 作答；问题不预设类型。
_Avoid_: 问答、聊天、对话

**提示词模板（Prompt）**：
每种 AI 能力对应的默认指令文本，含占位符，可在设置中逐条覆盖。
_Avoid_: prompt、指令、模板

**模型（Model）**：
执行 AI 能力的大语言模型，可以是内置预设或用户指定的自定义模型。
_Avoid_: 引擎、LLM、AI 服务商

### 运行时

**服务（Service）**：
挂在应用生命周期上的单例逻辑单元，一类持续行为对应一个服务。
_Avoid_: 管理器、控制器、store

**任务（Task）**：
一次异步 AI 调用在前端的句柄，带状态、结果与过期时间。
_Avoid_: 请求、作业、job

**消息（Message）**：
两个运行端点之间的一次调用，带方法名、参数与返回类型。
_Avoid_: 事件、接口、信号

**端点（Endpoint）**：
消息的发出或接收方，取 `extension`（后台）、`inject`（页面注入脚本）、`app`（面板界面）三者之一。
_Avoid_: 上下文、进程、模块

### 配置

**配置（EnvData）**：
用户的设置，跟随浏览器账号同步。
_Avoid_: 选项、设置、偏好、config

**临时数据（TempData）**：
随视频变化、只留在本地的界面状态，不参与同步。
_Avoid_: 状态、缓存、会话
