# 保留上游的存储键名

用户配置与临时数据以 `bilibili-subtitle_env`、`bilibili-subtitle_temp` 两个键存放在 `chrome.storage` 中。项目更名为 Gloss 时，这两个字符串被**故意保持不变**：改名会让所有已安装用户的既有配置静默丢失——读不到旧键等同于被当成全新用户。代价是代码里留着与产品名不符的字符串；将来真要改名，必须先实现「读旧键 → 写新键」的迁移逻辑。
