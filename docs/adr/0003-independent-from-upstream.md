# 与上游独立演进

项目源自 IndieKKY/bilibili-subtitle（MIT 许可），最初以其 fork 的形式存在。脱离为独立仓库 `jiahao07A/glosstake` 后（产品名 Gloss，`isFork: false`、`parent: null`），决策是**不跟踪上游、不合并上游变更**，独立演进；本地已移除 `fork` / `upstream` 两个 remote，只保留 `origin`。上游版权声明按 MIT 许可要求保留在 `LICENSE`（`Copyright 2023 IndieKKY`）。
